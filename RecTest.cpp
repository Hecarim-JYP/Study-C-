/*#include <iostream>

using namespace std;

int main()
{
	int a = 10, b = 20;
	int &aRef = a; // a를 참조하는 참조 변수 aRef를 선언
		cout << "a의 값 : " << a << endl;
	cout << "aRef가 참조하는 값 : " << aRef << endl << endl;
	aRef = 100; // aRef가 참조하는 값을 변경한다는 것은 결국 a의 값을 변경한다는 것과 같다.
	cout << "a의 값 : " << a << endl;
	aRef = b; // 위와 동일한 프로세스
	cout << "a의 값 : " << a << endl;
	return 0;
}
*/